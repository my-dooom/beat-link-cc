/**
 * @file beat_link_client.cpp
 * @brief Implementation file for beat_link_client.h.
 *
 * All logic is currently inline in the header (header-only template / inline
 * implementations). This translation unit is required so that CMake builds a
 * proper static library target; add any non-inline definitions here as needed.
 */

#include "beat_link_client.h"

// All implementations are currently defined inline in the header.
// This file intentionally contains no additional definitions.
